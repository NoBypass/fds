type Discord {
    discordId: String!
    name: String!
    level: Int
    xp: Int
    streak: Int
    lastDailyAt: Int
    linkedWith: LinkedWith
}

type LinkedWith {
    linkedAt: Int!
    player: Player!
    discord: Discord!
}

schema:
  - internal/pkg/schemas/*.graphqls

exec:
  filename: internal/pkg/generated/generated.go
  package: generated

model:
  filename: internal/pkg/generated/models/models_gen.go
  package: models

resolver:
  layout: follow-schema
  dir: internal/app/resolvers
  package: resolvers
  filename_template: "{name}.resolver.go"

autobind:

models:
